<!DOCTYPE html>   
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
</html>
  <body>
    <div class="container">
      <p class="subtitle">A small message board built with Express + EJS</p>

      <div class="header-actions">
        <span></span>
        <a href="/new" class="btn btn-primary">Write a new message</a>
      </div>

      <ul class="messages">
        <% messages.forEach((m) => { %>
          <li class="card message">
            <div class="message-title">
              <span class="message-user"><%= m.username %></span>
              <span class="message-time"><%= new Date(m.created_at).toLocaleString() %></span>
            </div>
            <div class="message-text"><%= m.body %></div>
            <div class="message-actions">
              <a class="btn btn-link" href="/messages/<%= m.id %>">Open →</a>
            </div>
          </li>
        <% }) %>
      </ul>
    </div>
  </body>
